#!/usr/bin/env bash
# One-time staging host bootstrap (Docker, compose plugin, dirs).
set -euo pipefail
echo "Install Docker Engine + Compose plugin, create app dir, clone repo."
echo "See docs/deploy/staging.md"
